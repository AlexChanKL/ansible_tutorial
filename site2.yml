all:
  hosts:
    children:
      windows:
        13.70.42.254:
      vars:
        ansible_connection: ssh
        ansible_user: alex
        ansible_ssh_pass: TDcs@0921TDcs
  
- name: update repository index (CentOS)
  tags: always
  dnf:
    update_cache: yes
  changed_when: false
  when: ansible_distribution == "CentOS"
